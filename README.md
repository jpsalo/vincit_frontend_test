# Vincit frontend test project

## Install packages

1. Make sure you are using at least node v4.x.x and npm 3.x.x.

2. Run in the project root directory:

`npm install`

## Build and run

Run

`npm start`

to fire up dev server and then open browser to http://localhost:3456
